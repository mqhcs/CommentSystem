// Code generated by ent, DO NOT EDIT.

package ent

import (
	"comment-main/app/comment-service/internal/data/ent/predicate"
	"comment-main/app/comment-service/internal/data/ent/replyindex"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ReplyIndexQuery is the builder for querying ReplyIndex entities.
type ReplyIndexQuery struct {
	config
	ctx        *QueryContext
	order      []replyindex.OrderOption
	inters     []Interceptor
	predicates []predicate.ReplyIndex
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReplyIndexQuery builder.
func (riq *ReplyIndexQuery) Where(ps ...predicate.ReplyIndex) *ReplyIndexQuery {
	riq.predicates = append(riq.predicates, ps...)
	return riq
}

// Limit the number of records to be returned by this query.
func (riq *ReplyIndexQuery) Limit(limit int) *ReplyIndexQuery {
	riq.ctx.Limit = &limit
	return riq
}

// Offset to start from.
func (riq *ReplyIndexQuery) Offset(offset int) *ReplyIndexQuery {
	riq.ctx.Offset = &offset
	return riq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (riq *ReplyIndexQuery) Unique(unique bool) *ReplyIndexQuery {
	riq.ctx.Unique = &unique
	return riq
}

// Order specifies how the records should be ordered.
func (riq *ReplyIndexQuery) Order(o ...replyindex.OrderOption) *ReplyIndexQuery {
	riq.order = append(riq.order, o...)
	return riq
}

// First returns the first ReplyIndex entity from the query.
// Returns a *NotFoundError when no ReplyIndex was found.
func (riq *ReplyIndexQuery) First(ctx context.Context) (*ReplyIndex, error) {
	nodes, err := riq.Limit(1).All(setContextOp(ctx, riq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{replyindex.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (riq *ReplyIndexQuery) FirstX(ctx context.Context) *ReplyIndex {
	node, err := riq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ReplyIndex ID from the query.
// Returns a *NotFoundError when no ReplyIndex ID was found.
func (riq *ReplyIndexQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = riq.Limit(1).IDs(setContextOp(ctx, riq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{replyindex.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (riq *ReplyIndexQuery) FirstIDX(ctx context.Context) int64 {
	id, err := riq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ReplyIndex entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ReplyIndex entity is found.
// Returns a *NotFoundError when no ReplyIndex entities are found.
func (riq *ReplyIndexQuery) Only(ctx context.Context) (*ReplyIndex, error) {
	nodes, err := riq.Limit(2).All(setContextOp(ctx, riq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{replyindex.Label}
	default:
		return nil, &NotSingularError{replyindex.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (riq *ReplyIndexQuery) OnlyX(ctx context.Context) *ReplyIndex {
	node, err := riq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ReplyIndex ID in the query.
// Returns a *NotSingularError when more than one ReplyIndex ID is found.
// Returns a *NotFoundError when no entities are found.
func (riq *ReplyIndexQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = riq.Limit(2).IDs(setContextOp(ctx, riq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{replyindex.Label}
	default:
		err = &NotSingularError{replyindex.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (riq *ReplyIndexQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := riq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ReplyIndexes.
func (riq *ReplyIndexQuery) All(ctx context.Context) ([]*ReplyIndex, error) {
	ctx = setContextOp(ctx, riq.ctx, "All")
	if err := riq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ReplyIndex, *ReplyIndexQuery]()
	return withInterceptors[[]*ReplyIndex](ctx, riq, qr, riq.inters)
}

// AllX is like All, but panics if an error occurs.
func (riq *ReplyIndexQuery) AllX(ctx context.Context) []*ReplyIndex {
	nodes, err := riq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ReplyIndex IDs.
func (riq *ReplyIndexQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if riq.ctx.Unique == nil && riq.path != nil {
		riq.Unique(true)
	}
	ctx = setContextOp(ctx, riq.ctx, "IDs")
	if err = riq.Select(replyindex.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (riq *ReplyIndexQuery) IDsX(ctx context.Context) []int64 {
	ids, err := riq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (riq *ReplyIndexQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, riq.ctx, "Count")
	if err := riq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, riq, querierCount[*ReplyIndexQuery](), riq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (riq *ReplyIndexQuery) CountX(ctx context.Context) int {
	count, err := riq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (riq *ReplyIndexQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, riq.ctx, "Exist")
	switch _, err := riq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (riq *ReplyIndexQuery) ExistX(ctx context.Context) bool {
	exist, err := riq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReplyIndexQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (riq *ReplyIndexQuery) Clone() *ReplyIndexQuery {
	if riq == nil {
		return nil
	}
	return &ReplyIndexQuery{
		config:     riq.config,
		ctx:        riq.ctx.Clone(),
		order:      append([]replyindex.OrderOption{}, riq.order...),
		inters:     append([]Interceptor{}, riq.inters...),
		predicates: append([]predicate.ReplyIndex{}, riq.predicates...),
		// clone intermediate query.
		sql:  riq.sql.Clone(),
		path: riq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Oid int64 `json:"oid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ReplyIndex.Query().
//		GroupBy(replyindex.FieldOid).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (riq *ReplyIndexQuery) GroupBy(field string, fields ...string) *ReplyIndexGroupBy {
	riq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ReplyIndexGroupBy{build: riq}
	grbuild.flds = &riq.ctx.Fields
	grbuild.label = replyindex.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Oid int64 `json:"oid,omitempty"`
//	}
//
//	client.ReplyIndex.Query().
//		Select(replyindex.FieldOid).
//		Scan(ctx, &v)
func (riq *ReplyIndexQuery) Select(fields ...string) *ReplyIndexSelect {
	riq.ctx.Fields = append(riq.ctx.Fields, fields...)
	sbuild := &ReplyIndexSelect{ReplyIndexQuery: riq}
	sbuild.label = replyindex.Label
	sbuild.flds, sbuild.scan = &riq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ReplyIndexSelect configured with the given aggregations.
func (riq *ReplyIndexQuery) Aggregate(fns ...AggregateFunc) *ReplyIndexSelect {
	return riq.Select().Aggregate(fns...)
}

func (riq *ReplyIndexQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range riq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, riq); err != nil {
				return err
			}
		}
	}
	for _, f := range riq.ctx.Fields {
		if !replyindex.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if riq.path != nil {
		prev, err := riq.path(ctx)
		if err != nil {
			return err
		}
		riq.sql = prev
	}
	return nil
}

func (riq *ReplyIndexQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ReplyIndex, error) {
	var (
		nodes = []*ReplyIndex{}
		_spec = riq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ReplyIndex).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ReplyIndex{config: riq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, riq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (riq *ReplyIndexQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := riq.querySpec()
	_spec.Node.Columns = riq.ctx.Fields
	if len(riq.ctx.Fields) > 0 {
		_spec.Unique = riq.ctx.Unique != nil && *riq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, riq.driver, _spec)
}

func (riq *ReplyIndexQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(replyindex.Table, replyindex.Columns, sqlgraph.NewFieldSpec(replyindex.FieldID, field.TypeInt64))
	_spec.From = riq.sql
	if unique := riq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if riq.path != nil {
		_spec.Unique = true
	}
	if fields := riq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, replyindex.FieldID)
		for i := range fields {
			if fields[i] != replyindex.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := riq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := riq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := riq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := riq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (riq *ReplyIndexQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(riq.driver.Dialect())
	t1 := builder.Table(replyindex.Table)
	columns := riq.ctx.Fields
	if len(columns) == 0 {
		columns = replyindex.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if riq.sql != nil {
		selector = riq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if riq.ctx.Unique != nil && *riq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range riq.predicates {
		p(selector)
	}
	for _, p := range riq.order {
		p(selector)
	}
	if offset := riq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := riq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ReplyIndexGroupBy is the group-by builder for ReplyIndex entities.
type ReplyIndexGroupBy struct {
	selector
	build *ReplyIndexQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rigb *ReplyIndexGroupBy) Aggregate(fns ...AggregateFunc) *ReplyIndexGroupBy {
	rigb.fns = append(rigb.fns, fns...)
	return rigb
}

// Scan applies the selector query and scans the result into the given value.
func (rigb *ReplyIndexGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rigb.build.ctx, "GroupBy")
	if err := rigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReplyIndexQuery, *ReplyIndexGroupBy](ctx, rigb.build, rigb, rigb.build.inters, v)
}

func (rigb *ReplyIndexGroupBy) sqlScan(ctx context.Context, root *ReplyIndexQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rigb.fns))
	for _, fn := range rigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rigb.flds)+len(rigb.fns))
		for _, f := range *rigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ReplyIndexSelect is the builder for selecting fields of ReplyIndex entities.
type ReplyIndexSelect struct {
	*ReplyIndexQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ris *ReplyIndexSelect) Aggregate(fns ...AggregateFunc) *ReplyIndexSelect {
	ris.fns = append(ris.fns, fns...)
	return ris
}

// Scan applies the selector query and scans the result into the given value.
func (ris *ReplyIndexSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ris.ctx, "Select")
	if err := ris.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReplyIndexQuery, *ReplyIndexSelect](ctx, ris.ReplyIndexQuery, ris, ris.inters, v)
}

func (ris *ReplyIndexSelect) sqlScan(ctx context.Context, root *ReplyIndexQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ris.fns))
	for _, fn := range ris.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ris.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ris.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
